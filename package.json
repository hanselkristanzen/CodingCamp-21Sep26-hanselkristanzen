{
  "name": "expense-budget-visualizer",
  "version": "1.0.0",
  "description": "Expense & Budget Visualizer — zero-dependency client-side SPA",
  "private": true,
  "scripts": {
    "test": "vitest --run"
  },
  "devDependencies": {
    "fast-check": "3.22.0",
    "jsdom": "24.1.3",
    "vitest": "2.1.9"
  }
}
